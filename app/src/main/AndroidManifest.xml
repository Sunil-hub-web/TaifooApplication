<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.taifooapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaifooApplication">
        <activity
            android:name=".activity.ProductDescription"
            android:theme="@style/Theme.TaifooApplication.NoActionBar">

        </activity>
        <activity
            android:name=".activity.OrderSuccessFully"
            android:theme="@style/Theme.TaifooApplication.NoActionBar"/>
        <activity
            android:name=".activity.HomePageActivity"
            android:theme="@style/Theme.TaifooApplication.NoActionBar"></activity>
        <activity
            android:name=".activity.SplashScreen"
            android:theme="@style/Theme.TaifooApplication.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/Theme.TaifooApplication.NoActionBar" />
    </application>

</manifest>